import type {
  GeometryScreen,
  StatisticsScreen,
  TrigScreen,
} from '../../types/calculator';

export type GeometryWorkspaceProps = {
  screen: GeometryScreen;
};

export type TrigonometryWorkspaceProps = {
  screen: TrigScreen;
};

export type StatisticsWorkspaceProps = {
  screen: StatisticsScreen;
};
